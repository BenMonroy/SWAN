<UserControl x:Class="SWAN.Views.RMFDashboardView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SWAN.Views" 
             xmlns:components="clr-namespace:SWAN.Components"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="CadetBlue">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock Grid.Row="0" Text="RMF Dashboard Page" FontSize="50"/>

        <!--TODO: -Jake
            Allow Checkboxes to be passed in as children to the expanders.
            Allow for easier reusability
            Allow for values of the checkboxes to be referenced and sustained across pages
        -->

        <components:ExpanderComponent Grid.Row="1"
                                 ColumnTitle="EXPANDER 1"
                                 ExpanderHeader="My Expander"
                                 ExpanderContent="THIS IS A TEST 1"
                                 HorizontalAlignment="Stretch"
                                 VerticalAlignment="Top"
                                 Margin="10"/>

        <components:ExpanderComponent Grid.Row="2"
                         ColumnTitle="EXPANDER 2"
                         ExpanderHeader="My Expander"
                         HorizontalAlignment="Stretch"
                         VerticalAlignment="Top"
                         Margin="10"/>

        <components:CheckboxComponent CheckboxLabel="Label 1" ThreeState="True" Grid.Row="3"/>

    </Grid>
</UserControl>