<UserControl x:Class="SWAN.Views.RiskScoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:SWAN.Views"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             xmlns:oxy="http://oxyplot.org/wpf"
             mc:Ignorable="d"
             Background="{DynamicResource WindowBackgroundColor}">

    <UserControl.Resources>
        <local:BooleanToCheckedConverter x:Key="BooleanToCheckedConverter"/>
    </UserControl.Resources>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Header Section -->
        <Grid Grid.Row="0" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <!-- Risk Score -->
            <StackPanel Grid.Column="0" HorizontalAlignment="Left" VerticalAlignment="Top">
                <TextBlock Text="Overall Risk Score" FontSize="18" FontWeight="Bold" Foreground="{DynamicResource PrimaryColor}"/>
                <TextBlock Text="{Binding RiskScore}" FontSize="36" FontWeight="Bold" Foreground="{DynamicResource AccentColor}"/>
            </StackPanel>

            <!-- Dashboard Title -->
            <TextBlock Grid.Column="1" Text="Risk Score Dashboard" FontSize="28" FontWeight="Bold" Foreground="{DynamicResource PrimaryColor}" HorizontalAlignment="Center"/>
        </Grid>

        <!-- Main Content -->
        <Grid Grid.Row="1" Margin="10">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="3*"/>
                <ColumnDefinition Width="2*"/>
            </Grid.ColumnDefinitions>

            <!-- Cyber Controls Graph -->
            <materialDesign:Card Grid.Column="0" Margin="10" Padding="10" UniformCornerRadius="10" Background="{DynamicResource CardBackgroundColor}" VerticalAlignment="Stretch">
                <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <TextBlock Text="Failed Standards per Cyber Control" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryColor}" Margin="0,0,0,10"/>
                    <oxy:PlotView Model="{Binding CyberControlBarGraphModel}" MinHeight="300" HorizontalAlignment="Stretch" VerticalAlignment="Stretch"/>
                </StackPanel>
            </materialDesign:Card>

            <!-- Pie Chart -->
            <materialDesign:Card Grid.Column="1" Margin="10" Padding="10" UniformCornerRadius="10" Background="{DynamicResource CardBackgroundColor}" VerticalAlignment="Stretch">
                <StackPanel HorizontalAlignment="Center" VerticalAlignment="Top">
                    <TextBlock Text="Vulnerabilities by Severity" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryColor}" Margin="0,0,0,10"/>
                    <oxy:PlotView Model="{Binding RiskSeverityModel}" Height="250" Width="250"/>
                </StackPanel>
            </materialDesign:Card>
        </Grid>

        <!-- Controls List -->
        <Grid Grid.Row="2" Margin="10">
            <materialDesign:Card Margin="10" Padding="10" UniformCornerRadius="10" Background="{DynamicResource CardBackgroundColor}">
                <StackPanel HorizontalAlignment="Stretch" VerticalAlignment="Stretch">
                    <TextBlock Text="All Controls from Dashboard" FontSize="24" FontWeight="Bold" Foreground="{DynamicResource PrimaryColor}" Margin="0,0,0,10"/>
                    <ScrollViewer VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Disabled" Height="200">
                        <ItemsControl ItemsSource="{Binding}">
                            <ItemsControl.ItemTemplate>
                                <DataTemplate>
                                    <Grid Margin="5">
                                        <Grid.ColumnDefinitions>
                                            <ColumnDefinition Width="3*"/>
                                            <ColumnDefinition Width="1*"/>
                                        </Grid.ColumnDefinitions>

                                        <!-- Control label -->
                                        <TextBlock Text="{Binding Label}" FontSize="16" Foreground="{DynamicResource PrimaryColor}" Grid.Column="0" TextTrimming="CharacterEllipsis" />

                                        <!-- Checked status -->
                                        <TextBlock Text="{Binding IsSelected, Converter={StaticResource BooleanToCheckedConverter}}" FontSize="16" Foreground="{DynamicResource PrimaryColor}" Grid.Column="1" HorizontalAlignment="Right"/>
                                    </Grid>
                                </DataTemplate>
                            </ItemsControl.ItemTemplate>
                        </ItemsControl>
                    </ScrollViewer>
                </StackPanel>
            </materialDesign:Card>
        </Grid>
    </Grid>
</UserControl>
