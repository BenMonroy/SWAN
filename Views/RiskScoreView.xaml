<UserControl x:Class="SWAN.Views.RiskScoreView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
             xmlns:local="clr-namespace:SWAN.Views" 
             xmlns:components="clr-namespace:SWAN.Components"
             xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
             mc:Ignorable="d" 
             d:DesignHeight="450" d:DesignWidth="800"
             Background="CadetBlue">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="Auto"/>
            <ColumnDefinition Width="*"/>
            <ColumnDefinition Width="Auto"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <TextBlock Grid.Row="0" Grid.Column="1" Text="Risk Score Page" FontSize="50" HorizontalAlignment="Left" VerticalAlignment="Top" Margin="10,0,0,0"/>

        <!-- First Material Design Card -->
        <materialDesign:Card Grid.Row="1" Grid.Column="1" Margin="10" Padding="10" UniformCornerRadius="20" HorizontalAlignment="Stretch">
            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top">
                <TextBlock Text="Overall Risk Score" FontWeight="SemiBold" FontSize="18" HorizontalAlignment="Left" Margin="10" />
                <TextBox materialDesign:HintAssist.Hint="Enter text here..." Width="145" Height="102"/>
            </StackPanel>
        </materialDesign:Card>

        <!-- Second Material Design Card -->
        <materialDesign:Card Grid.Row="2" Grid.Column="1" Margin="10" Padding="10" UniformCornerRadius="20" HorizontalAlignment="Stretch">
            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Top">
                <TextBlock Text="Risk by Severity " FontWeight="SemiBold" FontSize="18" HorizontalAlignment="Left" Margin="10" /> 
            </StackPanel>
        </materialDesign:Card>
        <TextBox materialDesign:HintAssist.Hint="Enter text here..." Grid.ColumnSpan="2" Margin="10,65,478,49" Grid.RowSpan="2" Grid.Row="2"/>
        <TextBlock Text="Risk Score Trend" FontWeight="SemiBold" FontSize="18" Grid.ColumnSpan="2" Margin="209,30,445,124" Grid.Row="1" />
        <TextBox materialDesign:HintAssist.Hint="Enter text here..." Grid.ColumnSpan="2" Margin="209,66,242,10" Grid.Row="1"/>
        <TextBlock Text="Risk by Framework" FontWeight="SemiBold" FontSize="18" Grid.ColumnSpan="2" Margin="342,25,290,49" Grid.Row="2" />
        <TextBox materialDesign:HintAssist.Hint="Enter text here..." Grid.ColumnSpan="2" Margin="383,65,10,49" Grid.RowSpan="2" Grid.Row="2"/>
    </Grid>
</UserControl>
